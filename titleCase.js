const titleCase  = function(text) {
    // Your code in here ...
    let sentence = [...text]

    for (let i = 0; i < sentence.length; i++){
        let x = i + 1
       // console.log(sentence[x])
        if(sentence[i] === ' ') {
           // sentence[x].toUpperCase()
            console.log[x]
        }
    }

    return sentence
  };

  console.log(titleCase("this is a test"))