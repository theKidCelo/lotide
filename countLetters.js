const assertEqual = function(actual, expected) {
    if(actual === expected){
        console.log(`✅✅✅assertion passed: ${actual} === ${expected}`)
    }
    if(actual !== expected){
        console.log(`🛑🛑🛑assertion passed: ${actual} !== ${expected}`)
    }
};

// TEST CODE
const countOnly = function(letters){
    let count = new Set(letters)

    let anwser = {}

    count.forEach(calue => {
        answer[value] = Array.filter((val) => val=== value).length
    })

    return answer
}

